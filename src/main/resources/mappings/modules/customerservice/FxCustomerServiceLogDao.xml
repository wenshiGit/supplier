<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.customerservice.dao.FxCustomerServiceLogDao">
    
	<sql id="fxCustomerServiceLogColumns">
		a.log_id AS "logId",
		a.create_time AS "createTime",
		a.log_desc AS "logDesc",
		a.read_state AS "readState",
		a.service_id AS "serviceId",
		a.service_state AS "serviceState",
		a.user_id AS "user.id",
		u7.name AS "user.name"
	</sql>
	
	<sql id="fxCustomerServiceLogJoins">
		LEFT JOIN sys_user u7 ON u7.id = a.user_id
	</sql>
    
	<select id="get" resultType="FxCustomerServiceLog">
		SELECT 
			<include refid="fxCustomerServiceLogColumns"/>
		FROM fx_customer_service_log a
		<include refid="fxCustomerServiceLogJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="FxCustomerServiceLog">
		SELECT 
			<include refid="fxCustomerServiceLogColumns"/>
		FROM fx_customer_service_log a
		<include refid="fxCustomerServiceLogJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="FxCustomerServiceLog">
		SELECT 
			<include refid="fxCustomerServiceLogColumns"/>
		FROM fx_customer_service_log a
		<include refid="fxCustomerServiceLogJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO fx_customer_service_log(
			log_id,
			create_time,
			log_desc,
			read_state,
			service_id,
			service_state,
			user_id
		) VALUES (
			#{logId},
			#{createTime},
			#{logDesc},
			#{readState},
			#{serviceId},
			#{serviceState},
			#{user.id}
		)
	</insert>
	
	<update id="update">
		UPDATE fx_customer_service_log SET 	
			log_id = #{logId},
			create_time = #{createTime},
			log_desc = #{logDesc},
			read_state = #{readState},
			service_id = #{serviceId},
			service_state = #{serviceState},
			user_id = #{user.id}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM fx_customer_service_log
		WHERE id = #{id}
	</update>
	
</mapper>