<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.customerservice.dao.FxOnlineQuestionsDao">
    
	<sql id="fxOnlineQuestionsColumns">
		a.id AS "id",
		a.order_id AS "orderId",
		a.question_time AS "questionTime",
		a.img_url AS "imgUrl",
		a.content AS "content",
		a.status AS "status",
		a.questioner AS "questioner",
		a.title AS "title",
		a.reply_people AS "replyPeople",
		a.reply_time AS "replyTime",
		a.solution_time AS "solutionTime",
		a.questioner_id AS "questionerId",
		a.reply_id AS "replyId"
	</sql>
	
	<sql id="fxOnlineQuestionsJoins">
	</sql>
    
	<select id="get" resultType="FxOnlineQuestions">
		SELECT 
			<include refid="fxOnlineQuestionsColumns"/>
		FROM fx_online_questions a
		<include refid="fxOnlineQuestionsJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="FxOnlineQuestions">
		SELECT 
			<include refid="fxOnlineQuestionsColumns"/>
		FROM fx_online_questions a
		<include refid="fxOnlineQuestionsJoins"/>
		<where>
			
			<if test="title != null and title != ''">
				AND a.title LIKE 
					<if test="dbName == 'oracle'">'%'||#{title}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{title}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{title},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="FxOnlineQuestions">
		SELECT 
			<include refid="fxOnlineQuestionsColumns"/>
		FROM fx_online_questions a
		<include refid="fxOnlineQuestionsJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO fx_online_questions(
			id,
			order_id,
			question_time,
			img_url,
			content,
			status,
			questioner,
			title,
			reply_people,
			reply_time,
			solution_time,
			questioner_id,
			reply_id
		) VALUES (
			#{id},
			#{orderId},
			#{questionTime},
			#{imgUrl},
			#{content},
			#{status},
			#{questioner},
			#{title},
			#{replyPeople},
			#{replyTime},
			#{solutionTime},
			#{questionerId},
			#{replyId}
		)
	</insert>
	
	<update id="update">
		UPDATE fx_online_questions SET 	
			order_id = #{orderId},
			question_time = #{questionTime},
			img_url = #{imgUrl},
			content = #{content},
			status = #{status},
			questioner = #{questioner},
			title = #{title},
			reply_people = #{replyPeople},
			reply_time = #{replyTime},
			solution_time = #{solutionTime},
			questioner_id = #{questionerId},
			reply_id = #{replyId}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM fx_online_questions
		WHERE id = #{id}
	</update>
	
</mapper>