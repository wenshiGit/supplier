<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.distribution.dao.FxOrdersLogDao">
    
	<sql id="fxOrdersLogColumns">
		a.log_id AS "logId",
		a.content AS "content",
		a.create_time AS "createTime",
		a.opt_user_id AS "optUserId",
		a.opt_user_name AS "optUserName",
		a.order_id AS "orderId",
		a.log_type AS "logType",
		a.rest_order_id AS "restOrderId"
	</sql>
	
	<sql id="fxOrdersLogJoins">
	</sql>
    
	<select id="get" resultType="FxOrdersLog">
		SELECT 
			<include refid="fxOrdersLogColumns"/>
		FROM fx_orders_log a
		<include refid="fxOrdersLogJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="FxOrdersLog">
		SELECT 
			<include refid="fxOrdersLogColumns"/>
		FROM fx_orders_log a
		<include refid="fxOrdersLogJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="FxOrdersLog">
		SELECT 
			<include refid="fxOrdersLogColumns"/>
		FROM fx_orders_log a
		<include refid="fxOrdersLogJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO fx_orders_log(
			log_id,
			content,
			create_time,
			opt_user_id,
			opt_user_name,
			order_id,
			log_type,
			rest_order_id
		) VALUES (
			#{logId},
			#{content},
			#{createTime},
			#{optUserId},
			#{optUserName},
			#{orderId},
			#{logType},
			#{restOrderId}
		)
	</insert>
	
	<update id="update">
		UPDATE fx_orders_log SET 	
			log_id = #{logId},
			content = #{content},
			create_time = #{createTime},
			opt_user_id = #{optUserId},
			opt_user_name = #{optUserName},
			order_id = #{orderId},
			log_type = #{logType},
			rest_order_id = #{restOrderId}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM fx_orders_log
		WHERE id = #{id}
	</update>
	
</mapper>