<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.finace.dao.FxOrdersTaobaoChargeDao">
    
	<sql id="fxOrdersTaobaoChargeColumns">
		a.id AS "id",
		a.buyer_nick AS "buyerNick",
		a.tid AS "tid",
		a.title AS "title",
		a.trade_money AS "tradeMoney",
		a.trade_time AS "tradeTime",
		a.pay_time AS "payTime",
		a.download_time AS "downloadTime",
		a.status AS "status",
		a.send_goods_date AS "sendGoodsDate",
		a.charge_date AS "chargeDate",
		a.charge_status AS "chargeStatus",
		a.charge_status_desc AS "chargeStatusDesc",
		a.user_id AS "user.id",
		u14.name AS "user.name"
	</sql>
	
	<sql id="fxOrdersTaobaoChargeJoins">
		LEFT JOIN sys_user u14 ON u14.id = a.user_id
	</sql>
    
	<select id="get" resultType="FxOrdersTaobaoCharge">
		SELECT 
			<include refid="fxOrdersTaobaoChargeColumns"/>
		FROM fx_orders_taobao_charge a
		<include refid="fxOrdersTaobaoChargeJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="FxOrdersTaobaoCharge">
		SELECT 
			<include refid="fxOrdersTaobaoChargeColumns"/>
		FROM fx_orders_taobao_charge a
		<include refid="fxOrdersTaobaoChargeJoins"/>
		<where>
			
			<if test="title != null and title != ''">
				AND a.title LIKE 
					<if test="dbName == 'oracle'">'%'||#{title}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{title}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{title},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="FxOrdersTaobaoCharge">
		SELECT 
			<include refid="fxOrdersTaobaoChargeColumns"/>
		FROM fx_orders_taobao_charge a
		<include refid="fxOrdersTaobaoChargeJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO fx_orders_taobao_charge(
			id,
			buyer_nick,
			tid,
			title,
			trade_money,
			trade_time,
			pay_time,
			download_time,
			status,
			send_goods_date,
			charge_date,
			charge_status,
			charge_status_desc,
			user_id
		) VALUES (
			#{id},
			#{buyerNick},
			#{tid},
			#{title},
			#{tradeMoney},
			#{tradeTime},
			#{payTime},
			#{downloadTime},
			#{status},
			#{sendGoodsDate},
			#{chargeDate},
			#{chargeStatus},
			#{chargeStatusDesc},
			#{user.id}
		)
	</insert>
	
	<update id="update">
		UPDATE fx_orders_taobao_charge SET 	
			buyer_nick = #{buyerNick},
			tid = #{tid},
			title = #{title},
			trade_money = #{tradeMoney},
			trade_time = #{tradeTime},
			pay_time = #{payTime},
			download_time = #{downloadTime},
			status = #{status},
			send_goods_date = #{sendGoodsDate},
			charge_date = #{chargeDate},
			charge_status = #{chargeStatus},
			charge_status_desc = #{chargeStatusDesc},
			user_id = #{user.id}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM fx_orders_taobao_charge
		WHERE id = #{id}
	</update>
	
</mapper>