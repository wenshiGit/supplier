<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.inventory.dao.SupInventoryDao">
    
	<sql id="supInventoryColumns">
		a.id AS "id",
		a.channel_id AS "channelId",
		a.inven_agio AS "invenAgio",
		a.inven_date AS "invenDate",
		a.inven_desc AS "invenDesc",
		a.inner_product_no AS "innerProductNo",
		a.inner_size AS "innerSize",
		a.qty AS "qty",
		a.inven_price AS "invenPrice",
		a.status AS "status",
		a.standard_size AS "standardSize",
		a.create_by AS "createBy.id",
		a.create_time AS "createTime",
		a.update_by AS "updateBy.id",
		a.update_time AS "updateTime",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="supInventoryJoins">
	</sql>
	
		<!-- 插入库存 -->
	<insert id="batchInsert" >
		INSERT INTO sup_inventory(
			id,
			channel_id,
			inven_agio,
			inven_date,
			inven_desc,
			inner_product_no,
			inner_size,
			qty,
			inven_price,
			status,
			standard_size,
			create_by,
			create_time,
			update_by,
			update_time,
			remarks,
			del_flag
		)
		 VALUES
		<foreach collection="list" item="item" index="index"  separator=",">
			(
			#{item.id},
			#{item.channelId},
			#{item.invenAgio},
			#{item.invenDate},
			#{item.invenDesc},
			#{item.innerProductNo},
			#{item.innerSize},
			#{item.qty},
			#{item.invenPrice},
			#{item.status},
			#{item.standardSize},
			#{item.createBy.id},
			#{item.createTime},
			#{item.updateBy.id},
			#{item.updateTime},
			#{item.remarks},
			#{item.delFlag}
			)
		</foreach>
	</insert>
	
	<!-- 根据channelId删除渠道库存 -->
	<delete id="deleteByChannelId" >
		DELETE FROM sup_inventory WHERE channel_id = #{channelId}
	</delete>
    
	<select id="get" resultType="SupInventory">
		SELECT 
			<include refid="supInventoryColumns"/>
		FROM sup_inventory a
		<include refid="supInventoryJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="SupInventory">
		SELECT 
			<include refid="supInventoryColumns"/>
		FROM sup_inventory a
		<include refid="supInventoryJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="innerProductNo != null and innerProductNo != ''">
				AND a.inner_product_no = #{innerProductNo}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="SupInventory">
		SELECT 
			<include refid="supInventoryColumns"/>
		FROM sup_inventory a
		<include refid="supInventoryJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO sup_inventory(
			id,
			channel_id,
			inven_agio,
			inven_date,
			inven_desc,
			inner_product_no,
			inner_size,
			qty,
			inven_price,
			status,
			standard_size,
			create_by,
			create_time,
			update_by,
			update_time,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{channelId},
			#{invenAgio},
			#{invenDate},
			#{invenDesc},
			#{innerProductNo},
			#{innerSize},
			#{qty},
			#{invenPrice},
			#{status},
			#{standardSize},
			#{createBy.id},
			#{createTime},
			#{updateBy.id},
			#{updateTime},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE sup_inventory SET 	
			channel_id = #{channelId},
			inven_agio = #{invenAgio},
			inven_date = #{invenDate},
			inven_desc = #{invenDesc},
			inner_product_no = #{innerProductNo},
			inner_size = #{innerSize},
			qty = #{qty},
			inven_price = #{invenPrice},
			status = #{status},
			standard_size = #{standardSize},
			create_time = #{createTime},
			update_by = #{updateBy.id},
			update_time = #{updateTime},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE sup_inventory SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>