<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.invocie.dao.FxInvocieDetailDao">
    
	<sql id="fxInvocieDetailColumns">
		a.invocie_detail_id AS "invocieDetailId",
		a.goods_count AS "goodsCount",
		a.goods_nbr AS "goodsNbr",
		a.goods_price AS "goodsPrice",
		a.goods_size AS "goodsSize",
		a.goods_title AS "goodsTitle",
		a.invocie_id AS "invocieId",
		a.order_id AS "orderId",
		a.state AS "state"
	</sql>
	
	<sql id="fxInvocieDetailJoins">
	</sql>
    
	<select id="get" resultType="FxInvocieDetail">
		SELECT 
			<include refid="fxInvocieDetailColumns"/>
		FROM fx_invocie_detail a
		<include refid="fxInvocieDetailJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="FxInvocieDetail">
		SELECT 
			<include refid="fxInvocieDetailColumns"/>
		FROM fx_invocie_detail a
		<include refid="fxInvocieDetailJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="FxInvocieDetail">
		SELECT 
			<include refid="fxInvocieDetailColumns"/>
		FROM fx_invocie_detail a
		<include refid="fxInvocieDetailJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO fx_invocie_detail(
			invocie_detail_id,
			goods_count,
			goods_nbr,
			goods_price,
			goods_size,
			goods_title,
			invocie_id,
			order_id,
			state
		) VALUES (
			#{invocieDetailId},
			#{goodsCount},
			#{goodsNbr},
			#{goodsPrice},
			#{goodsSize},
			#{goodsTitle},
			#{invocieId},
			#{orderId},
			#{state}
		)
	</insert>
	
	<update id="update">
		UPDATE fx_invocie_detail SET 	
			invocie_detail_id = #{invocieDetailId},
			goods_count = #{goodsCount},
			goods_nbr = #{goodsNbr},
			goods_price = #{goodsPrice},
			goods_size = #{goodsSize},
			goods_title = #{goodsTitle},
			invocie_id = #{invocieId},
			order_id = #{orderId},
			state = #{state}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM fx_invocie_detail
		WHERE id = #{id}
	</update>
	
</mapper>