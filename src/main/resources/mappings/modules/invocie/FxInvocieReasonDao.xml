<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.invocie.dao.FxInvocieReasonDao">
    
	<sql id="fxInvocieReasonColumns">
		a.id AS "id",
		a.invocie_id AS "invocieId",
		a.context AS "context",
		a.create_time AS "createTime",
		a.opt_user_id AS "optUserId"
	</sql>
	
	<sql id="fxInvocieReasonJoins">
	</sql>
    
	<select id="get" resultType="FxInvocieReason">
		SELECT 
			<include refid="fxInvocieReasonColumns"/>
		FROM fx_invocie_reason a
		<include refid="fxInvocieReasonJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="FxInvocieReason">
		SELECT 
			<include refid="fxInvocieReasonColumns"/>
		FROM fx_invocie_reason a
		<include refid="fxInvocieReasonJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="FxInvocieReason">
		SELECT 
			<include refid="fxInvocieReasonColumns"/>
		FROM fx_invocie_reason a
		<include refid="fxInvocieReasonJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO fx_invocie_reason(
			id,
			invocie_id,
			context,
			create_time,
			opt_user_id
		) VALUES (
			#{id},
			#{invocieId},
			#{context},
			#{createTime},
			#{optUserId}
		)
	</insert>
	
	<update id="update">
		UPDATE fx_invocie_reason SET 	
			invocie_id = #{invocieId},
			context = #{context},
			create_time = #{createTime},
			opt_user_id = #{optUserId}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM fx_invocie_reason
		WHERE id = #{id}
	</update>
	
</mapper>