<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.notice.dao.FxBulletinReadDao">
    
	<sql id="fxBulletinReadColumns">
		a.id AS "id",
		a.bulletin_id AS "bulletinId",
		a.create_time AS "createTime",
		a.user_id AS "user.id",
		u4.name AS "user.name"
	</sql>
	
	<sql id="fxBulletinReadJoins">
		LEFT JOIN sys_user u4 ON u4.id = a.user_id
	</sql>
    
	<select id="get" resultType="FxBulletinRead">
		SELECT 
			<include refid="fxBulletinReadColumns"/>
		FROM fx_bulletin_read a
		<include refid="fxBulletinReadJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="FxBulletinRead">
		SELECT 
			<include refid="fxBulletinReadColumns"/>
		FROM fx_bulletin_read a
		<include refid="fxBulletinReadJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="FxBulletinRead">
		SELECT 
			<include refid="fxBulletinReadColumns"/>
		FROM fx_bulletin_read a
		<include refid="fxBulletinReadJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO fx_bulletin_read(
			id,
			bulletin_id,
			create_time,
			user_id
		) VALUES (
			#{id},
			#{bulletinId},
			#{createTime},
			#{user.id}
		)
	</insert>
	
	<update id="update">
		UPDATE fx_bulletin_read SET 	
			bulletin_id = #{bulletinId},
			create_time = #{createTime},
			user_id = #{user.id}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM fx_bulletin_read
		WHERE id = #{id}
	</update>
	
</mapper>