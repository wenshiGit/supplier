<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.sys.dao.FxAreaDao">
    
	<sql id="fxAreaColumns">
		a.area_id AS "area.id",
		a.area_name AS "areaName",
		a.area_name_alias AS "areaNameAlias",
		a.area_type AS "areaType",
		a.parent_area_id AS "parentAreaId",
		a.zip AS "zip",
		a1.name AS "area.name"
	</sql>
	
	<sql id="fxAreaJoins">
		LEFT JOIN sys_area a1 ON a1.id = a.area_id
	</sql>
    
	<select id="get" resultType="FxArea">
		SELECT 
			<include refid="fxAreaColumns"/>
		FROM fx_area a
		<include refid="fxAreaJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="FxArea">
		SELECT 
			<include refid="fxAreaColumns"/>
		FROM fx_area a
		<include refid="fxAreaJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="FxArea">
		SELECT 
			<include refid="fxAreaColumns"/>
		FROM fx_area a
		<include refid="fxAreaJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO fx_area(
			area_id,
			area_name,
			area_name_alias,
			area_type,
			parent_area_id,
			zip
		) VALUES (
			#{area.id},
			#{areaName},
			#{areaNameAlias},
			#{areaType},
			#{parentAreaId},
			#{zip}
		)
	</insert>
	
	<update id="update">
		UPDATE fx_area SET 	
			area_id = #{area.id},
			area_name = #{areaName},
			area_name_alias = #{areaNameAlias},
			area_type = #{areaType},
			parent_area_id = #{parentAreaId},
			zip = #{zip}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM fx_area
		WHERE id = #{id}
	</update>
	
</mapper>