<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.sys.dao.FxBrandAliasDao">
    
	<sql id="fxBrandAliasColumns">
		a.brand_id AS "brandId",
		a.brand_alias AS "brandAlias",
		a.brand_alias_id AS "brandAliasId"
	</sql>
	
	<sql id="fxBrandAliasJoins">
	</sql>
    
	<select id="get" resultType="FxBrandAlias">
		SELECT 
			<include refid="fxBrandAliasColumns"/>
		FROM fx_brand_alias a
		<include refid="fxBrandAliasJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="FxBrandAlias">
		SELECT 
			<include refid="fxBrandAliasColumns"/>
		FROM fx_brand_alias a
		<include refid="fxBrandAliasJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="FxBrandAlias">
		SELECT 
			<include refid="fxBrandAliasColumns"/>
		FROM fx_brand_alias a
		<include refid="fxBrandAliasJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO fx_brand_alias(
			brand_id,
			brand_alias,
			brand_alias_id
		) VALUES (
			#{brandId},
			#{brandAlias},
			#{brandAliasId}
		)
	</insert>
	
	<update id="update">
		UPDATE fx_brand_alias SET 	
			brand_id = #{brandId},
			brand_alias = #{brandAlias},
			brand_alias_id = #{brandAliasId}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM fx_brand_alias
		WHERE id = #{id}
	</update>
	
</mapper>