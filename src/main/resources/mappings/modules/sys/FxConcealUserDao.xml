<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.sys.dao.FxConcealUserDao">
    
	<sql id="fxConcealUserColumns">
		a.id AS "id",
		a.create_time AS "createTime",
		a.conceal_rule_id AS "concealRuleId",
		a.user_id AS "user.id",
		u4.name AS "user.name"
	</sql>
	
	<sql id="fxConcealUserJoins">
		LEFT JOIN sys_user u4 ON u4.id = a.user_id
	</sql>
    
	<select id="get" resultType="FxConcealUser">
		SELECT 
			<include refid="fxConcealUserColumns"/>
		FROM fx_conceal_user a
		<include refid="fxConcealUserJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="FxConcealUser">
		SELECT 
			<include refid="fxConcealUserColumns"/>
		FROM fx_conceal_user a
		<include refid="fxConcealUserJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="FxConcealUser">
		SELECT 
			<include refid="fxConcealUserColumns"/>
		FROM fx_conceal_user a
		<include refid="fxConcealUserJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO fx_conceal_user(
			id,
			create_time,
			conceal_rule_id,
			user_id
		) VALUES (
			#{id},
			#{createTime},
			#{concealRuleId},
			#{user.id}
		)
	</insert>
	
	<update id="update">
		UPDATE fx_conceal_user SET 	
			create_time = #{createTime},
			conceal_rule_id = #{concealRuleId},
			user_id = #{user.id}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM fx_conceal_user
		WHERE id = #{id}
	</update>
	
</mapper>