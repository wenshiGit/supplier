<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.sys.dao.FxExpressDefaultPriceDao">
    
	<sql id="fxExpressDefaultPriceColumns">
		a.id AS "id",
		a.express_name AS "expressName",
		a.express_id AS "expressId",
		a.area_id AS "area.id",
		a.first_price AS "firstPrice",
		a.add_price AS "addPrice",
		a4.name AS "area.name"
	</sql>
	
	<sql id="fxExpressDefaultPriceJoins">
		LEFT JOIN sys_area a4 ON a4.id = a.area_id
	</sql>
    
	<select id="get" resultType="FxExpressDefaultPrice">
		SELECT 
			<include refid="fxExpressDefaultPriceColumns"/>
		FROM fx_express_default_price a
		<include refid="fxExpressDefaultPriceJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="FxExpressDefaultPrice">
		SELECT 
			<include refid="fxExpressDefaultPriceColumns"/>
		FROM fx_express_default_price a
		<include refid="fxExpressDefaultPriceJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="FxExpressDefaultPrice">
		SELECT 
			<include refid="fxExpressDefaultPriceColumns"/>
		FROM fx_express_default_price a
		<include refid="fxExpressDefaultPriceJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO fx_express_default_price(
			id,
			express_name,
			express_id,
			area_id,
			first_price,
			add_price
		) VALUES (
			#{id},
			#{expressName},
			#{expressId},
			#{area.id},
			#{firstPrice},
			#{addPrice}
		)
	</insert>
	
	<update id="update">
		UPDATE fx_express_default_price SET 	
			express_name = #{expressName},
			express_id = #{expressId},
			area_id = #{area.id},
			first_price = #{firstPrice},
			add_price = #{addPrice}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM fx_express_default_price
		WHERE id = #{id}
	</update>
	
</mapper>