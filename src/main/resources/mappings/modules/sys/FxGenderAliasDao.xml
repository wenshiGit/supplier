<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.sys.dao.FxGenderAliasDao">
    
	<sql id="fxGenderAliasColumns">
		a.gender_alias_id AS "genderAliasId",
		a.gender_id AS "genderId",
		a.gender_alias AS "genderAlias"
	</sql>
	
	<sql id="fxGenderAliasJoins">
	</sql>
    
	<select id="get" resultType="FxGenderAlias">
		SELECT 
			<include refid="fxGenderAliasColumns"/>
		FROM fx_gender_alias a
		<include refid="fxGenderAliasJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="FxGenderAlias">
		SELECT 
			<include refid="fxGenderAliasColumns"/>
		FROM fx_gender_alias a
		<include refid="fxGenderAliasJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="FxGenderAlias">
		SELECT 
			<include refid="fxGenderAliasColumns"/>
		FROM fx_gender_alias a
		<include refid="fxGenderAliasJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO fx_gender_alias(
			gender_alias_id,
			gender_id,
			gender_alias
		) VALUES (
			#{genderAliasId},
			#{genderId},
			#{genderAlias}
		)
	</insert>
	
	<update id="update">
		UPDATE fx_gender_alias SET 	
			gender_alias_id = #{genderAliasId},
			gender_id = #{genderId},
			gender_alias = #{genderAlias}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM fx_gender_alias
		WHERE id = #{id}
	</update>
	
</mapper>