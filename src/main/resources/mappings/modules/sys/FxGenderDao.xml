<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.sys.dao.FxGenderDao">
    
	<sql id="fxGenderColumns">
		a.gender_id AS "genderId",
		a.gender_name AS "genderName"
	</sql>
	
	<sql id="fxGenderJoins">
	</sql>
    
	<select id="get" resultType="FxGender">
		SELECT 
			<include refid="fxGenderColumns"/>
		FROM fx_gender a
		<include refid="fxGenderJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="FxGender">
		SELECT 
			<include refid="fxGenderColumns"/>
		FROM fx_gender a
		<include refid="fxGenderJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="FxGender">
		SELECT 
			<include refid="fxGenderColumns"/>
		FROM fx_gender a
		<include refid="fxGenderJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO fx_gender(
			gender_id,
			gender_name
		) VALUES (
			#{genderId},
			#{genderName}
		)
	</insert>
	
	<update id="update">
		UPDATE fx_gender SET 	
			gender_id = #{genderId},
			gender_name = #{genderName}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM fx_gender
		WHERE id = #{id}
	</update>
	
</mapper>