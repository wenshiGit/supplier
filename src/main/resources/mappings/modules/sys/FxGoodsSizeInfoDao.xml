<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.sys.dao.FxGoodsSizeInfoDao">
    
	<sql id="fxGoodsSizeInfoColumns">
		a.id AS "id",
		a.brand_id AS "brandId",
		a.brand_name AS "brandName",
		a.goods_size AS "goodsSize",
		a.goods_size_name AS "goodsSizeName",
		a.goods_type_id AS "goodsTypeId",
		a.goods_type AS "goodsType",
		a.order_id AS "orderId",
		a.series AS "series",
		a.sex AS "sex",
		a.sex_id AS "sexId",
		a.standard_goods_size AS "standardGoodsSize",
		a.brand_size_id AS "brandSizeId"
	</sql>
	
	<sql id="fxGoodsSizeInfoJoins">
	</sql>
    
	<select id="get" resultType="FxGoodsSizeInfo">
		SELECT 
			<include refid="fxGoodsSizeInfoColumns"/>
		FROM fx_goods_size_info a
		<include refid="fxGoodsSizeInfoJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="FxGoodsSizeInfo">
		SELECT 
			<include refid="fxGoodsSizeInfoColumns"/>
		FROM fx_goods_size_info a
		<include refid="fxGoodsSizeInfoJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="FxGoodsSizeInfo">
		SELECT 
			<include refid="fxGoodsSizeInfoColumns"/>
		FROM fx_goods_size_info a
		<include refid="fxGoodsSizeInfoJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO fx_goods_size_info(
			id,
			brand_id,
			brand_name,
			goods_size,
			goods_size_name,
			goods_type_id,
			goods_type,
			order_id,
			series,
			sex,
			sex_id,
			standard_goods_size,
			brand_size_id
		) VALUES (
			#{id},
			#{brandId},
			#{brandName},
			#{goodsSize},
			#{goodsSizeName},
			#{goodsTypeId},
			#{goodsType},
			#{orderId},
			#{series},
			#{sex},
			#{sexId},
			#{standardGoodsSize},
			#{brandSizeId}
		)
	</insert>
	
	<update id="update">
		UPDATE fx_goods_size_info SET 	
			brand_id = #{brandId},
			brand_name = #{brandName},
			goods_size = #{goodsSize},
			goods_size_name = #{goodsSizeName},
			goods_type_id = #{goodsTypeId},
			goods_type = #{goodsType},
			order_id = #{orderId},
			series = #{series},
			sex = #{sex},
			sex_id = #{sexId},
			standard_goods_size = #{standardGoodsSize},
			brand_size_id = #{brandSizeId}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM fx_goods_size_info
		WHERE id = #{id}
	</update>
	
</mapper>